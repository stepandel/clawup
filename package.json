{
  "name": "clawup-root",
  "private": true,
  "packageManager": "pnpm@10.29.2",
  "description": "Clawup monorepo — deploy fleets of AI agents",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "build:cli-infra": "bash scripts/bundle-infra.sh"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@scarf/scarf",
      "esbuild"
    ]
  },
  "pulumi": {
    "resource": true
  }
}
